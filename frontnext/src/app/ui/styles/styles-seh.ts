// ../styles/sumstyles.ts
const sumstyles = {
  container: 'fixed bottom-0 left-0 right-0 p-4', // 배경색 제거
  innerContainer: 'flex justify-center items-center w-full gap-4',
  textCommon: 'text-md font-medium', // 크기를 'text-md'로 조정
  select: 'px-3 py-2 border rounded focus:outline-none', // 셀렉트 박스 패딩 조정
  divider: 'w-full border-t border-gray-500 my-2', // 진한 색상으로 변경
};

export default sumstyles;

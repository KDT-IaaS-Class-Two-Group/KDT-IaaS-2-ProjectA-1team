import React, { useState } from 'react';

export const Toggle = () => {
  const [toggle, setToggle] = useState(false);

  const showToggle = () => {
    to;
  };

  return (
    <div>
      <div>children</div>
      <button>▼</button>
    </div>
  );
};
